# Cleanup Tasks

## Deprecated Code

### `internal/ui/filelist.go` - FileListModel

The `FileListModel` type is no longer used. It has been replaced by `FileListWidget` which wraps `teapot.SelectableList`.

**Can be safely deleted.**
