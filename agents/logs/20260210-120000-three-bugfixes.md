# Three bug fixes for branch-range-selection

| Field       | Value                        |
|-------------|------------------------------|
| Strategy    | Bug Fix                      |
| Complexity  | Simple                       |
| Started     | 2026-02-10 12:00             |
| Ended       | 2026-02-10 12:05             |
| Outcome     | Completed                    |

## Changes

### 1. Skip WebSocket reload while typing in TipTap editor
- `App.tsx`: Added early return in `handleWebSocketMessage` when `document.activeElement` is inside `.tiptap`

### 2. Scroll to conversation line when clicking in FileList
- `FileList.tsx`: Added `onScrollToLine` prop, called on conversation entry click with `conv.lineNumber`
- `App.tsx`: Added `handleScrollToLine` callback that sets `restoreLineNo`, passed to `FileList`

### 3. Deduplicate git ancestry checks in GetConversations
- `branch.go`: Added `AncestryCache` type that memoizes `merge-base --is-ancestor` results; added `IsCommitInRangeCached` and `ClosestBranchForSHACached` variants
- `get_conversations.go`: Merged two-pass filter+resolve into single loop sharing one `AncestryCache`

## Obstacles
None.
